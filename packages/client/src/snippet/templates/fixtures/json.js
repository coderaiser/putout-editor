export default `// remove-duplicate-keywords (package.json plugin)
// The JSON processor wraps package.json fields as a function call.

__putout_processor_json({
    "keywords": ["putout", "putout", "codemod"]
});
`;
